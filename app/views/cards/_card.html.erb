<li 
data-id="<%= card.id %>" 
data-idx="<%= card.sort_idx %>"
<% if !!catagory %>
data-catagory-id="<%= catagory.id %>"
<% end %>
>

	<a class="card-link" href="<%= card_url(card) %>">

  <div class="card-text card">
		<div class="option_icon">
			<%= image_tag("option_icon.png")%>
		</div>
    <%= card.title %>
  </div>
	</a>
</li>
