<!DOCTYPE html>
<html>
<head>
  <title>Clairvoyance</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <style>
  header {
    width: 900px;
    padding: 20px;
  }

  header li {
    padding-right: 10px;
    float: left;
  }

  header li > h4 {
    padding-right: 15px;
    float: left;
  }

  header li > nav {
    float: left;
  }

  article {
    display: table;
  }

  .group:after {
    content: "";
    clear: both;
    display: table;
  }

  .logo {
    margin-right: 40px;
    float: left;
  }

  .board-nav {
    float: left;
  }

  .login {
    float: right;
  }

  .card {
    height: 120px;
    width: 120px;
    border: solid black 1px;
    margin: 10px;
    float: left;
  }

  .card-set {
    width: 500px;
  }
  </style>

</head>
<body>
  <header class="group">
    <div class="logo">
      <a href="/">CV</a>
    </div>

    <% if logged_in? %>
    <nav class="board-nav">
      <ul class="group">
        <% current_user.member_boards.each do |board| %>
        <li><%= link_to "#{board.title}", board_url(board) %></li>
        <% end %>
      </ul>
    </nav>
    <% end %>

    <nav class="login">
    	<ul class="group">
    		<% if logged_in? %>
    		<li>Hello <%= current_user.email %></li>
    		<li><%= button_to "Logout", session_url, method: :delete %></li>
    		<% else %>
    		<li><%= link_to "Sign Up", new_user_url %></li>
    		<li><%= link_to "Login", new_session_url %></li>
    		<% end %>
    	</ul>
    </nav>

  </header>
	<%= flash[:errors] %>

  <article id="finished"></article>
  <article id="current"></article>
  <article id="pending"></article>
  <footer></footer>

<%= yield %>

</body>
</html>
