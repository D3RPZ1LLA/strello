<h1>Your Boards</h1>
<table>
	<% @boards.each do |board| %>
	<tr>
		<td><%= link_to "#{board.title}", board_url(board) %></td>
	</tr>
	<% end %>
</table>
<br>
<%= link_to "New Board", new_board_url %>